{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "pip install -r requirements.txt && python src/manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "cd src && gunicorn config.wsgi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}